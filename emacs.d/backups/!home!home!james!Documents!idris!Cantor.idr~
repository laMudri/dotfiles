module Cantor
  %default total

  data Bit = O | I
  Cantor : Type
  Cantor = Stream Bit

  mutual
    find : (Cantor -> Bool) -> Cantor
    forSome : (Cantor -> Bool) -> Bool
    forEvery : (Cantor -> Bool) -> Bool
